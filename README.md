# SMART_BMI
SMART_BMI is a simple Android application that calculates the Body Mass Index (BMI) based on user input (height and weight) . The app uses user-friendly UI elements and logic to categorize the BMI result (e.g., underweight, normal, overweight, obese).
